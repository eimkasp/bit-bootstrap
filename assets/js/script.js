$(document).ready(function () {
    // Priskiriame HTML elementui, slick functionaluma
    $(".bit-slider").slick({
        slidesToShow: 3,
        dots: true,
        centerMode: true,
        centerPadding: '60px',
      });
});